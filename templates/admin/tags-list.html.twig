{% extends 'layout/layout-admin.html.twig' %}

{% block header %}<a class="navbar-brand" href="{{ url('tagsList') }}">Tags</a>{% endblock %}

{% block content %}

    <div class="col-md-12">
        <div class="card strpied-tabled-with-hover">
            <div class="card-header ">
                <h4 class="card-title">Tags</h4>
                <a href="{{ url('tagsCreate') }}">New</a>
            </div>
            <div class="card-body table-full-width table-responsive">
                <table class="table table-hover table-striped">
                    <thead>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Type</th>
                    </thead>
                    <tbody>

                    {% for element in list %}

                        <tr>
                            <td>{{ element.id }}</td>
                            <td>{{ element.name }}</td>
                            <td>{{ element.type }}</td>
                            <td>
                                <a style="margin-right: 20px" href="{{ url('tagsEdit', {id: element.id}) }}">Edit</a>
                                <a href="{{ url('tagsDelete', {id: element.id}) }}">Delete</a>
                            </td>
                        </tr>

                    {% endfor %}

                    </tbody>
                </table>
            </div>
        </div>
    </div>

{% endblock %}